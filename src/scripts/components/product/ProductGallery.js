// import $ from 'jquery';
const Flickity = require('flickity-as-nav-for');

class ProductGallery {
  constructor(el) {
    this.init(el);
  }

  init(el) {
    this.el = el;
  }
}

export default ProductGallery;
